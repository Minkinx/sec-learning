# 静态脱敏 (Static Data Masking, SDM)

## 定义与工作流程

**静态脱敏 (Static Data Masking, SDM)** 是指在数据离开生产环境之前，对敏感数据进行一次性、不可逆的脱敏处理，生成脱敏后的数据集供非生产环境使用。SDM 的核心原则是"源端脱敏"——在数据导出时即完成转换，确保敏感信息不会以明文形式离开安全边界。

## ETML 流程

SDM 的标准流程可概括为 **Extract-Transform-Mask-Load (ETML)**：

1. **Extract** — 从源数据库导出生产数据，通常通过快照或增量导出方式获取。
2. **Transform** — 应用数据转换规则，包括格式转换、编码转换、参照完整性维护等预处理。
3. **Mask** — 执行核心脱敏操作，包括替换、乱序、空值化、加噪等算法。
4. **Load** — 将脱敏后的数据加载到目标环境（开发、测试、培训环境）。

## 生产到非生产流程

SDM 的典型应用场景是从生产环境向非生产环境复制数据。关键考量包括：

- **数据子集化** — 仅提取与测试/开发相关的数据子集，减少脱敏成本和存储开销。
- **参照完整性维护** — 确保脱敏后的外键关系保持一致，避免数据关联断裂。
- **脱敏一致性** — 同一敏感值在不同表中必须脱敏为相同的替代值（确定性脱敏）。

## 批量 vs 实时 SDM

| 类型 | 特点 | 适用场景 |
|------|------|----------|
| **批量 SDM** | 定时执行，处理大批量数据，吞吐量高 | 每日/每周的测试数据刷新 |
| **实时 SDM** | 按需触发，低延迟，集成到 ETL pipeline | 数据湖/数据仓库的持续同步 |

## 工具对比

| 工具 | 部署模式 | 数据库支持 | 特色功能 |
|------|----------|------------|----------|
| Delphix | 虚拟化 + SDM | 多平台 | 数据虚拟化、自动化 pipeline |
| IBM InfoSphere Optim | 企业级 | 大型机 + 分布式 | 数据子集化、合规报告 |
| Oracle Data Masking | 集成 | Oracle 生态 | Oracle DB 原生集成 |
| Informatica Persistent Data Masking | 独立/集成 | 广泛支持 | 算法丰富、调度灵活 |

**最佳实践建议**：SDM 应作为 CI/CD pipeline 的一环自动化执行，并与数据目录工具集成以实现脱敏策略的可追溯性。
